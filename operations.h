#ifndef OPERATIONS
#define OPERATIONS

#include <stdio.h>
#include <stdlib.h>

#include "types.h"
#include "utils.h"

void insert(Playlist*);
void removeMusic(Playlist*);
void list(Playlist*);
void listByMusic(Playlist*);

#endif